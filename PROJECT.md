# الشفرة — مرجع المشروع

**الدومين:** `alshafra.com`

**آخر تحديث:** 2026-08-18

هذه الوثيقة هي المرجع المعتمد للحالة الحالية. أُلغي منها وصف التوسع المؤتمت القديم؛ ملفات الخطط والتقارير التاريخية في `docs/` ليست تعليمات نشر.

## الهوية والنطاق

الهوية الأم هي **«الشفرة — حلول وأدوات ومراجع عملية»**، وتضم منتجين أوليين:

1. **تقويم السعودية:** المواعيد والرواتب والتاريخ الهجري والدراسي والإجازات والأدوات الحالية.
2. **الشفرة إصلاح:** دليل عالمي تدريجي لأكواد أعطال الأجهزة والصيانة المنزلية، وشعاره التحريري «افهم الكود قبل أن تطلب الفني».

لا تُنقل مسارات تقويم السعودية القديمة إلى بادئة جديدة. المسارات المفيدة مثل `/salaries` و`/hijri-calendar` و`/school-calendar` و`/holidays` و`/date-converter` و`/age-calculator` و`/countdown/*` تبقى مستقرة.

مسارات الإصلاح الجديدة:

```text
/fault-codes
/fault-codes/{device}
/fault-codes/{device}/{brand}
/fault-codes/{device}/{brand}/{code}
```

البداية التحريرية عربية، بغسالات Samsung وLG. لا تُضاف لغة أو صفحة كود إلى الفهرس قبل مراجعة بشرية ومصدر أولي ونطاق موديل واضح.

## التقنية

- React 18 + TypeScript + Vite.
- Tailwind CSS و`lucide-react`.
- موجّه خفيف مخصص يعتمد `history.pushState` في `src/lib/router.ts`.
- Supabase PostgreSQL/Auth/Storage للوحة التحرير.
- توليد ثابت بعد Vite عبر `scripts/prerender.mjs`.
- تصدير CMS وقت البناء إلى `src/data/cms-content.json`.

قاعدة Supabase اختيارية محليًا: إذا غابت متغيرات Vite تعرض `/admin` تعليمات الربط، وتبقى الصفحات العامة عاملة من البيانات المراجعة المتتبعة.

## الملفات الأساسية

```text
src/App.tsx                         توزيع الصفحات والتحميل الكسول
src/lib/router.ts                  تحليل المسار
src/lib/seo.ts                     title/meta/canonical/robots/JSON-LD
src/lib/faultCodes.ts              أنواع ومسارات أكواد الأعطال
src/lib/cms.ts                     أنواع CMS وMarkdown المقيد
src/lib/supabase.ts                عميل المتصفح بالمفتاح العام فقط
src/data/fault-codes.json          الأكواد المحلية المراجعة
src/data/cms-content.json          كاش النشر الثابت من Supabase
src/data/published-paths.json      أهداف الروابط الداخلية المسموحة
src/pages/FaultCodesPage.tsx       المحور والتصنيفات وصفحة الكود
src/pages/AdminPage.tsx            الدخول والقائمة والمحرر والوسائط والمراجعات
supabase/migrations/...sql         الجداول وRLS والأدوار والتخزين
scripts/sync-cms.mjs               تصدير السجلات المنشورة فقط
scripts/sync-published-paths.mjs   إعداد قائمة الروابط للواجهة
scripts/prerender.mjs              HTML ثابت وSEO وخريطة الموقع
scripts/validate-build.mjs         فحص الروابط وhreflang وrobots وsitemap
```

## لوحة التحرير

المسار `/admin` خاص ويحمل `noindex, nofollow, noarchive` في meta وترويسة الاستضافة، ولا يُحمّل إعلان AdSense.

الأدوار:

- `viewer`: بلا كتابة.
- `author`: ينشئ ويعدل مسوداته.
- `editor`: يراجع وينشر ويؤرشف.
- `admin`: يدير الأدوار والحذف والتدقيق.

لا يوجد تسجيل عام. ينشئ المالك المستخدمين من Supabase Auth ويرقّي الدور يدويًا. المفتاح `SUPABASE_SERVICE_ROLE_KEY` للخادم/البناء فقط، ويحظر وضعه في متغير يبدأ بـ`VITE_`.

المخطط يدعم المحتوى، المراجعات، الوسائط وحقوقها، المصادر، الترجمات، التحويلات، مواعيد الإتاحة وسجل التدقيق. المحرر الحالي يوفر العنوان والوصف والمسار واللغة والنص والمصادر وصورة الغلاف والترخيص والحالة والفهرسة والجدولة واستعادة مراجعة كمسودة.

راجع `docs/CMS_SETUP.md` للتثبيت والتشغيل والنسخ الاحتياطي.

## دورة النشر

```text
مسودة → مراجعة → منشور في القاعدة → بناء مراجع → صفحة ثابتة عامة
```

`npm run build` ينفذ بالترتيب:

1. `cms:sync`: يقرأ السجلات ذات `status=published` و`indexable=true` والتي حل `published_at` الخاص بها.
2. `publication-paths:sync`: يبني قائمة أهداف الروابط المسموحة.
3. `vite build`.
4. `prerender.mjs`: يولد HTML وsitemap وبيان النشر.
5. `validate:build`: يوقف البناء عند رابط مكسور أو `undefined` أو hreflang غير صالح أو تعارض robots أو اختلاف sitemap.

لا يستطيع البناء إضافة صفحات أو إرسال IndexNow أو تنفيذ commit آلي. تعريف GitHub الزمني القديم ما زال في ملف workflow لأن اتصال المستودع الحالي لا يملك صلاحية تعديل workflows؛ يوقفه `sync-cms.mjs` صراحة قبل البناء أو commit. يجب حذف الجدولة من GitHub بعد منح الاتصال صلاحية workflows. لا يساوي وجود تعريف في `scripts/catalog.mjs` السماح بالنشر؛ صفحات الكتالوج القديمة مجمدة على بيان النشر السابق حتى مراجعة كل URL.

## سياسة الروابط والفهرسة

- `Link.tsx` لا يخرج `<a href>` لمسار داخلي خارج مجموعة النشر المراجعة؛ يظهر النص بلا رابط بدل إنشاء 404.
- `sitemap.xml` لا يحتوي إلا canonical منشورًا ومفهرسًا ومولدًا فعليًا.
- الإدارة والأخطاء والمعاينات والمسودات لا تدخل sitemap أو `published.json`.
- لا تستخدم `changefreq` أو `priority`؛ `lastmod` يعكس تغيرًا فعليًا أو تاريخ مراجعة.
- hreflang لا يصدر إلا لبدائل منشورة ومتبادلة، وتُنشأ الوسوم بصيغة HTML الصحيحة.
- لا يوجد حد كلمات لأغراض الترتيب. البوابة بنيوية: عنوان ووصف وH1 وcanonical وrobots، وتحتاج المقالات المراجعة إلى أقسام ومصادر.
- المسار غير المعروف يجب أن يعيد 404 حقيقيًا من الاستضافة؛ لا تضف SPA catch-all يعيد 200.

لا تحوّل URL محذوفًا إلى الرئيسية تلقائيًا. كل URL يأخذ قرارًا منفردًا: إبقاء/تحديث، إعادة كتابة، دمج، `noindex`، أرشفة، 301 إلى بديل مكافئ، أو 404/410.

## قواعد محتوى أكواد الأعطال

كل صفحة كود يجب أن تشمل:

- نوع الجهاز والعلامة والرمز ونطاق الموديل.
- إجابة مختصرة لا توحي بأن الرمز موحد بين الأجهزة.
- تحذير سلامة وحدود لما يفعله المستخدم.
- أسباب مرتبة وفحوص خارجية آمنة.
- جدول تشخيص وإشارات توقف ومتى يلزم الفني.
- أسئلة شائعة ومصدر شركة/كتيب رسمي.
- تاريخ نشر ومراجعة.

الكتيب المطابق لرقم الموديل والدعم المحلي للشركة هما المرجع النهائي. لا توصي بفتح الجهاز أو تجاوز وسائل الأمان.

## Markdown الآمن

محتوى CMS يدعم فقط:

- `##` عنوان قسم.
- `###` عنوان فرعي.
- `-` أو `*` قائمة.
- فقرات نصية.

HTML الخام يعامل كنص، ولا يُستخدم `dangerouslySetInnerHTML` لمحتوى المحرر.

## الأوامر

```bash
npm run dev                    # خادم Vite المحلي
npm run typecheck              # TypeScript
npm run lint                   # ESLint
npm run cms:sync               # تصدير CMS (يحافظ على الكاش بلا أسرار محليًا)
npm run publication-paths:sync
npm run build                  # البناء والتحقق الكامل
npm run validate:build         # تدقيق dist موجود
```

التحقق قبل الدمج: `typecheck` و`lint` و`build` يجب أن تنجح، ثم يُراجع diff وبيان النشر بصريًا.

## قرارات لم تُستكمل تلقائيًا

- صفحات الكتالوج القديمة في بيان النشر ما زالت تحتاج قرار URL منفردًا بالاستناد إلى Search Console والروابط الخلفية والجودة والملاءمة.
- التوسع الإنجليزي لا يبدأ قبل استقرار العربية والـCMS، ولا تستخدم ترجمة fallback كصفحة مفهرسة.
- التحول المستقبلي إلى Next.js ممكن، لكنه ليس سببًا لتغيير URL أو الهوية والمحتوى دفعة واحدة.
- البداية ممكنة بخطط مجانية، لكن قاعدة ووسائط عالمية كبيرة لن تبقى بلا حدود أو تكلفة إلى الأبد.
