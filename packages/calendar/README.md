# @alshafra/calendar

Hijri (Umm Al-Qura) conversion, Riyadh “today”, and the Saudi weekend payday rule.

**Owns:** date math only.  
**Does not own:** React UI, JSON content, HTTP, salaries copy.

**Consumers:** `apps/web` (legacy Vite), future Astro islands, `@alshafra/tools`.

**Dependencies:** none.
