# @alshafra/seo

Public SEO engine (chat مرحلة 9 / Phase 7).

Canonical, robots, intent-first titles (no automatic `| Alshafra`), breadcrumbs, OG/Twitter, JSON-LD `@graph`, sitemap index, 410 registry, hreflang *ready* (silent until two real locales exist).

Does not store documents. Does not emit SearchAction until `/search` exists. Does not invent JobPosting/Product schema.
