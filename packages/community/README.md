# @alshafra/community

Q&A + comments + votes + reports. **Flags default off.** Public writes 404 until `community_enabled` and the matching surface flag are on. UGC stays `noindex` unless a human later overrides (`seo.ugc_auto_index` stays false).
