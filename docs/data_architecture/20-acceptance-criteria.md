# 20 — Acceptance Criteria

- [x] Database package with SQL migrations, constraints, indexes, RLS
- [x] documents + types + categories + tags + authors + entities + sources + revisions + SEO
- [x] Idempotent legacy import
- [x] 127 URLs as routes/documents
- [x] Tools represented
- [x] Analytics event contracts + zeroed metrics tables
- [x] Feature flags + settings
- [x] Secrets not committed
- [ ] Hosted Supabase migration — **not claimed** (no project in CI)
- Quality gates: `npm run typecheck`, `lint`, `test`, `build` must be run and reported honestly
