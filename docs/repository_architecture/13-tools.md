# 13 — Developer tools (`tools/`)

`check-boundaries.mjs` — deep imports, ui→database/social, service role in `apps/web/src`.

Not a workspace package. Not the user tools platform (`packages/tools`).
