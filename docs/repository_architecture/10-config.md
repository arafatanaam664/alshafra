# 10 — Config

- Root `package.json` orchestrates workspaces.  
- `.env.example` lists names only.  
- `.gitignore` ignores `.env` and `.env.*` except `.env.example`.  
- `@alshafra/config` lists server-only keys for boundary scans.  
- No secrets in git.
