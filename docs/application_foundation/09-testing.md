# 09 — Testing

- `tests/legacy/urls.mjs` — 127 unique paths in published.json  
- `tests/legacy/astro-dist.mjs` — each path has dist HTML with title, canonical, h1, not noindex  
- `tools/check-boundaries.mjs`  
- Golden routes included in the 127 check: `/`, `/date-converter`, `/hijri-calendar`, `/today`, `/salaries`, `/school-calendar`, `/holidays`, `/age-calculator`, `/articles`
