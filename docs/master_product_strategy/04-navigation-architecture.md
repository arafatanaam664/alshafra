# 04 — Navigation

السلوك الإلزامي:
- اسم القسم → صفحة القسم.
- السهم → فتح/إغلاق الفروع.
- الفرع → صفحته.
- HTML-first، بدون hover-only، RTL، مناسب للمس.

المصدر: الكتالوج بعد التجاوزات. لا تُعرض إلا الأقسام `enabled && hasPublicPage && showInNav`.
