# 01 — الحالة الحالية (من الكود، 2026-08-24)

المصدر = المستودع على الفرع `arena/01a02128-alshafra`، آخر التزام منشور قبل هذا التصحيح: Phase 12 `1d9dc59`. الإنتاج على `main` ما زال Vite.

## ما هو حي للجمهور

- 127 مسارًا عربيًا من `published.json` بدون تغيير.
- صفحة قسم `/calendar` ومركز `/tools` و`/tool/:slug`.
- `/trending/*` و`/articles/*` و`/search` (noindex).
- الصفحة الرئيسية تتحدث عن المنصة لا عن الهجرة.

## ما هو موجود تقنيًا ومطفأ

- CMS + Admin + RBAC + revisions.
- Media/R2 adapter.
- Community engine + Turnstile (الكتابة العامة 404).
- Feature flags: المجتمع، الوظائف، المنح، السفر، المقارنات، الحلول، AI، الإعلانات = false.
- التقويم والأدوات والترندات = true.

## قاعدة البيانات

- 11 migrations، ~67 جدولًا، RLS على 66.
- لا جدول `sections` منفصل. الكتالوج في `@alshafra/content` والتجاوزات في `site_settings.platform.sections`.

## التعارض السابق مع الرؤية

الصفحة الرئيسية كانت تشرح قرارات داخلية (الموقع السابق، Bing، شركة عالمية، ثمانية أعمدة). هذا أُزيل من الواجهة العامة.
