# 13 — SEO strategy

Arabic-first. User-first. لا نصوص داخل الصفحة لأجل محرك البحث فقط.
الحفاظ على 127 مسارًا وcanonical الذاتي.
الجودة قبل الفهرسة: كلمات كافية، وصف، مصادر في YMYL.
UGC لا يُفهرس تلقائيًا.
لا صفحات دول رقيقة. لا `/category` `/languages` `/news`.
