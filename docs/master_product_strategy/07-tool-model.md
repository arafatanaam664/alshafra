# 07 — Tool model

الحساب في `@alshafra/tools` و`@alshafra/calendar` بلا قاعدة بيانات داخل المحرك.
الأدوات القديمة تبقى على مساراتها. الجديدة على `/tool/:slug`.
الإدارة الحالية: قائمة + مقاييس صفرية. إنشاء أداة جديدة كاملة = Phase لاحقة.
