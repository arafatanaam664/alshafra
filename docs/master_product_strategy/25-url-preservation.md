# 25 — حفظ المسارات

127/127 تبقى 200 وself-canonical إلى أن يوجد 301 موثّق.
HIGH: `/` `/date-converter` `/hijri-calendar` `/today` `/salaries` `/school-calendar` `/holidays` `/countdown/*` `/age-calculator` `/articles/*`.
`/calendar` و`/tools` إضافيان وليسا بديلاً عن القديمة.
