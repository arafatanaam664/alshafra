# 16 — Testing

`npm run test:cms` — workflow, sanitizer, SEO audit, author cannot publish, 401 without cookie, login, dashboard, 127 URL parity after CMS publish.

`npm test` includes cms after Phase 4 data tests.
