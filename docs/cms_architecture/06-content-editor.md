# 06 — Content editor

Universal editor keyed by `document_type`. Body is `body_json` blocks (sanitized). Tabs: edit, SEO, FAQ, sources, history.

Slug/path change on published HIGH URLs requires `seo.edit_high_intent` and optional 301 `redirects` row (`createRedirect`).
