# 07 — Revisions

`document_revisions` immutable. Created on create, explicit save, publish, restore — not on autosave keystrokes.

Restore copies snapshot into working document and inserts a new revision with `restored_from_version`.
