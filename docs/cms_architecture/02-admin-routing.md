# 02 — Admin routing

Hash routes in the SPA (`#/content/:id`). API under `/api/v1/admin/*`.

Pages: Dashboard, Content list/new/edit, Taxonomy, Tools, Media, SEO/redirects, Analytics, Users, Flags, Settings, Audit, Health.

Community / Social / Automation: Coming soon — no live endpoints.
