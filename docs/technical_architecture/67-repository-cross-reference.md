# 67 — Repository cross-reference (Phase 2)

Phase 2 implemented the monorepo in `docs/repository_architecture/`.

**Amendment to `41-frontend-architecture.md`:** `apps/web` currently hosts the **legacy Vite + prerender** public site. Astro is still the target (H3) but is not the production renderer yet (Phase 2 ADR-203).
