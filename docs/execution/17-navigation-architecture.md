# Phase 17 — Navigation

```
Alshafra
├── التقويم والمواعيد → /calendar
├── الأدوات → /tools
├── الأدلة → /trending
└── المقالات → /articles
```

- Section name opens the hub.
- Arrow opens children.
- Breadcrumbs: الرئيسية → القسم → الصفحة (`packages/seo/src/breadcrumbs.ts`).
- Footer repeats hubs, calendar children, tools, legal.
- Feature flags still hide unfinished sections.
- 127 legacy URLs are unchanged.
