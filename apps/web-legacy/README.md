# @alshafra/web

Public site. **Phase 2:** the existing Vite + React + prerender app lives here so the 127 URLs and `npm run build` keep working.

Astro + islands (Phase 1 H3) is the target renderer; it is **not** swapped in this phase. Do not delete prerender until an Astro replacement passes legacy SEO tests.
