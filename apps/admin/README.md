# @alshafra/admin

Alshafra CMS control center. `noindex`. Server-only database access via Vite middleware (`server/`).

```
npm run dev:admin
```

Dev login (non-production): `editor@local.test` when `ADMIN_DEV_LOGIN=true`.
