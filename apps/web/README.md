# @alshafra/web (Astro)

Public site — Astro 5 static + React islands.

```
npm run dev      # from repo root
npm run build
```

Legacy Vite: `@alshafra/web-legacy` (`npm run dev:legacy` / `build:legacy`).
